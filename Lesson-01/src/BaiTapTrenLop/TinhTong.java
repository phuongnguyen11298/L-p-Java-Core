package BaiTapTrenLop;

import java.sql.SQLOutput;

public class TinhTong {
    public static void main (String [] args) {
        int a = 24;
        int b = 3;
        int c = a + b;
        int d = a - b;
        int e = a / b;
        float f = 0.1f;
        float g = a / f;
        System.out.println("Kết quả của phép tính " + a + "+" + b + " bằng " + c);
        System.out.println("Kết quả của phép tính " + a + "-" + b + " bằng " + d);
        System.out.println("Kết quả của phép tính " + a + "/" + b + " bằng " + e);
        System.out.println("Kết quả của phép tính " + a + "/" + f + " bằng " + g);
    }
}
